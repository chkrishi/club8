name: club8
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.0.3
  flutter_sound: ^9.28.0
  rename_app: ^1.4.0
  dio: ^5.9.0
  permission_handler: ^12.0.1
  just_audio: ^0.10.5
  record: ^6.1.2
  camera: ^0.11.3
  just_waveform: ^0.0.7
  video_player: ^2.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/8clublogo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/8clublogo.png
